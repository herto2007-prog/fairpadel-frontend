[build]
builder = "nixpacks"
buildCommand = "cd v2/backend && npm install && npm run build"

[deploy]
startCommand = "cd v2/backend && node scripts/reset-migrations.js && npx prisma migrate deploy && npx prisma db seed && npm run start:prod"
restartPolicyType = "on_failure"
restartPolicyMaxRetries": 10
